{
  "name": "interstellar-voyage-simulator",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:ts": "tsc -p tsconfig.json",
    "package:static": "node scripts/package-static.mjs",
    "build": "npm run build:ts && npm run package:static",
    "test": "npm run test:unit && npm run smoke",
    "check": "npm run test",
    "smoke": "npm run build && node --test tests/static-smoke.test.mjs",
    "test:unit": "npm run build:ts && node --test tests/relativity.test.mjs tests/simulation.test.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  }
}
